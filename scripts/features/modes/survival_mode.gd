extends GameMode
## El juego de siempre: oleadas infinitas, se termina cuando te matan.
##
## Existe aunque no haga NADA, y eso es a propósito: re-expresar el juego base a
## través de la abstracción es la única forma de saber si la abstracción alcanza.
## Si algún día este archivo necesita métodos para reproducir el juego de hoy, es
## que el contrato de `GameMode` está mal.

func _setup(_params: Dictionary) -> void:
	pass
